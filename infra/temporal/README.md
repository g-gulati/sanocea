# Temporal Boundary

Phase 0 includes a deterministic Temporal-shaped workflow harness in
`sanocea.workers.workflow`.

The production replacement should map the same business concepts to real
Temporal workflows, activities, signals, timers, search attributes, and worker
versioning.

